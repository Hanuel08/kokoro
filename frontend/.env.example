# Frontend Configuration (VITE_ prefixed vars are exposed to the browser)
# API keys should ONLY go in the backend .env file, not here

# Backend API URL (leave empty to use Vite proxy in dev, or set for production)
# VITE_API_URL=http://localhost:4000
